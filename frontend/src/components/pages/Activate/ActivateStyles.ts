import { makeStyles } from "tss-react/mui";

export const useActivateStyles = makeStyles()((theme) => ({
  wrapper: {
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "column",
  },
}));
